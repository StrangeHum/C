#include<stdio.h>
#include<stdlib.h>
int main(void)
{
    // int *pa, a = 3;
    // int *pb, b = 5;
    // pa = &a;
    // pb = &b;
    // printf("\n%d, %d", *pa, *pb);
    // printf("\n%p, %p", pa, pb);
    // int dds = *pa++;
    // printf("\n%d, %d", *pa, *pb);
    // printf("\n%p, %p", pa, pb);
    // // int dds = (*pa)++;
    // // printf("\n%d, %d", *pa, *pb);
    // // printf("\n%p, %p", pa, pb);
    return 0;
}
